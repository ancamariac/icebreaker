#ifndef GLOBALS_HPP
#define GLOBALS_HPP

#include <vector>
#include <Projectile.hpp>

namespace Globals{
    //std::vector<Projectile*> * prvec;
    std::vector<Projectile*> * getPrvec();
    void init();
};

#endif // GLOBALS_HPP
